{
    "layer": "top",
    "spacing": 13,
	"margin-top": 9,
	"margin-left": 9,
	"margin-right": 9,
	"margin-bottom": 0,
	
	"reload_style_on_change": true,

    "modules-left": ["hyprland/workspaces"],
    "modules-center": ["hyprland/window"],
    "modules-right": ["tray","pulseaudio","backlight","network","clock"],

	"hyprland/window": {
		"format": "{class}",
	},

    "tray": {
        "icon-size": 15,
        "spacing": 10
    },

    "clock": {
        // "timezone": "America/New_York",
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>",
		"format": " {:%H:%M}",
        "format-alt": "{:%Y-%m-%d}"
    },

    "backlight": {
        "device": "acpi_video0",
        "format": "{icon} {percent}%",
        "format-icons": [""],
        "tooltip": false,
    },

    "network": {
        "format-wifi": " {essid}",
        "format-ethernet": "{ipaddr}/{cidr}",
        "format-linked": "{ifname} (No IP)",
        "format-disconnected": " Disconnected",
        "tooltip": false,
    },

    "pulseaudio": {
        "format": "{icon} {volume}%",
        "format-muted": "󰖁",
        "format-icons": {
            "default": [""]
        },
        "on-click": "pactl set-sink-mute @DEFAULT_SINK@ toggle",
        "tooltip": false,
    },
}
